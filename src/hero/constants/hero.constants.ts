export const HERO_NAME_MAXLENGTH = 255;

export const FETCH_HERO_PARAM_ID = {
  name: 'id',
  type: String,
  description: 'MongoDB ObjectId of specific hero',
};

export const HERO_ID_NOT_FOUND = 'Hero does not exist';
